spring.application.name=back-end

# ---- DATABASE CONFIGURATION ----

# // My h2 in-memory database connection for testing
# spring.datasource.url=jdbc:h2:mem:testdb
# spring.datasource.driver-class-name=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.jpa.hibernate.ddl-auto=create-drop
# spring.h2.console.enabled=true

# ---- MysQL real database connection ----
spring.datasource.url=jdbc:mysql://172.21.73.226/cms?usessl=false
spring.datasource.username=root

spring.datasource.password=s4ynjaMms2oPSeend9ON8Wk1
spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# Inject data from data.sql
spring.sql.init.mode=always

# // My flappdoodle embedded MongoDB for testing
# spring.data.mongodb.port=27017
# spring.data.mongodb.database=testdb

# ---- MongoDB real database connection ----
spring.data.mongodb.uri=mongodb://root:7eq5EbS7SAQ0HFTQmIFy4xtC@172.21.124.101:27017/prescriptions?authSource=admin"

management.endpoint.health.show-details=always
management.health.db.enabled=true


api.path=/
jwt.secret=$!@#$^%$$$%####$DDCPN0234FCFDPD8670M


# This setting tells Spring Boot where to look for static assets (e.g. CSS, JS, images). Example: A CSS file in src/main/resources/static/css/style.css will be accessible at http://localhost:8080/css/style.css.
spring.web.resources.static-locations=classpath:/static/

# -------------------------
# Thymeleaf Configuration
# -------------------------
# Defines the base directory for Thymeleaf templates
spring.thymeleaf.prefix=classpath:/templates/
# Defines that all templates are ending with html. You can return "admin/adminDashboard" and Spring will resolve it to admin/adminDashboard.html.
spring.thymeleaf.suffix=.html
# Defines what rendering is used for the templates
spring.thymeleaf.mode=HTML
# Disables catching of templates. Useful during development to see changes immediately without restarting the app.
spring.thymeleaf.cache=false
# Ensures that all templates are read using UTF-8 encoding to support internationalization and special characters.  
spring.thymeleaf.encoding=UTF-8
